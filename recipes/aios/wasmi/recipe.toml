# Runtime WASM wasmi — promovido de Redox upstream recipes/wip/wasm/wasmi (Onda 7e)
# Fornece wasmi_cli no target Redox para validação/packaging de skills .wasm

[source]
git = "https://github.com/wasmi-labs/wasmi"

[build]
template = "custom"
script = """
cookbook_cargo_packages wasmi_cli
"""
