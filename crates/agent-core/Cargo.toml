[package]
name = "agent-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Agent/Skill model for Redox AIOS daemons"

[lib]
path = "src/lib.rs"

[dependencies]
memory-core = { path = "../memory-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
