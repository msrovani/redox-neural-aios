[package]
name = "memory-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Cliente unificado memory: / sgdbd (TCP + scheme file) — Redox Neural AIOS Fase 1"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
