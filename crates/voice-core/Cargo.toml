[package]
name = "voice-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Voice pipeline core for Redox AIOS (wake/STT/TTS userspace)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
