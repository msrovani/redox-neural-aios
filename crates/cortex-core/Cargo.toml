[package]
name = "cortex-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Cortex inference core for Redox AIOS (LLM/MoE userspace)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
