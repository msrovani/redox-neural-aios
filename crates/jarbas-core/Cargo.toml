[package]
name = "jarbas-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Jarbas shell AI core — session, soul, HUD (Redox AIOS Fase 5)"

[dependencies]
i18n-core = { path = "../i18n-core" }
voice-core = { path = "../voice-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
